[package]
name = "vibe-ecs-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
vibe-scene = { path = "../scene" }
vibe-physics = { path = "../physics" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
log = "0.4"

[dev-dependencies]
